import { T as Token } from '../../index-C5qB2fsf.js';

type MSEntraErrorResponse = {
    error: string;
    error_description: string;
    error_codes: number[];
};
type MSEntraTokenResponse = {
    access_token: string;
    expires_in: number;
    scope: string;
    token_type: string;
    id_token: string;
    refresh_token: string;
};
type MSEntraUser = {
    id: string;
    upn: string;
    verified_email: boolean;
    name: string;
    given_name: string;
    family_name: string;
    picture: string;
    local: string;
    employeeId: string;
};
type MSEntraToken = Token & {
    refresh_token?: string;
};

export type { MSEntraErrorResponse, MSEntraToken, MSEntraTokenResponse, MSEntraUser };

import { MiddlewareHandler } from 'hono';
import { MSEntraTokenResponse, MSEntraUser } from './types.js';
export { MSEntraErrorResponse, MSEntraToken } from './types.js';
import { O as OAuthVariables } from '../../index-C5qB2fsf.js';

declare function msentraAuth(options: {
    client_id?: string;
    client_secret?: string;
    tenant_id?: string;
    redirect_uri?: string;
    code?: string | undefined;
    scope: string[];
    state?: string;
}): MiddlewareHandler;

declare function refreshToken({ client_id, client_secret, tenant_id, refresh_token, }: {
    client_id: string;
    client_secret: string;
    tenant_id: string;
    refresh_token: string;
}): Promise<MSEntraTokenResponse>;

declare module 'hono' {
    interface ContextVariableMap extends OAuthVariables {
        'user-msentra': Partial<MSEntraUser> | undefined;
    }
}

export { MSEntraTokenResponse, MSEntraUser, msentraAuth, refreshToken };
